const $ = require('jquery');
const http = require('http');


let project = {

  scene: '',

  openProjects(_scene) {

    this.scene = _scene;

    // Need it to be up to nine most recent projects.


  },

  closeProjects(_scene) {

    this.scene = _scene;

  }



}
